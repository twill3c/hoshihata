# SPEC.md — hoshihata（道の駅 星畑）

<!-- scaffold template v1.12.0 から展開(2026-08-27)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

**架空の**道の駅「星畑（ほしはた）」（長野県南佐久郡南牧村 野辺山）の Web サイト。
ポートフォリオ作品であり、実在の施設ではない。

架空の施設には写真が存在しない。実在地の写真素材で埋めると実在誤認を招くため、
**ビジュアルは一切の写真を持たず SVG のみで組む**。その制約を弱点でなく主題に変えるために、
本サイトは「絵を描く」のではなく **公開データから計算した結果を描く**。
中心となるのは、野辺山の気象平年値から導く**旬カレンダー**である。

## 2. 施設設定（正本）

数字を動かすときは、まずこの節を直す。実装は `src/data/station.ts` だけがこれを持ち、
各ページは必ずそこから引く。二重に書かない。

| 項目 | 値 | 備考 |
|---|---|---|
| 名称 | 道の駅 星畑（ほしはた） | 架空 |
| 所在 | 長野県南佐久郡南牧村 野辺山 | **番地を持たない** |
| 電話 | **持たない** | F-03 |
| 標高 | 1,350 m | 気象庁アメダス野辺山観測所の標高に合わせる |
| 気象の基準点 | アメダス野辺山（prec_no=48 / block_no=0415） | 北緯 35°56.9′ 東経 138°28.3′ |
| 主要施設 | 直売所 / レストラン / ウッドデッキ / RV パーク / 情報コーナー | キャンプ場は**隣接施設**として案内のみ |

野辺山高原そのものについての事実（南牧村公式・気象庁）:

- 南牧村に**実在の道の駅は無い**（2026-08-27 確認）。本作が実在施設と衝突する余地はない
- 年平均気温は平年値で **7.25 ℃**（アメダス野辺山 1991–2020 の日別平年値を平均した実測値）。
  南牧村公式は「1 年の平均気温は 8 度前後」と記す。観測所と村域の代表点の違いとみなし、
  **サイト上は「年平均 7 ℃台」までしか主張しない**
- 日平均気温が 30 ℃ 以上になる日は平年値上 **0 日**

## 3. 機能要求

| ID | 要求 | 優先度 | ループ |
|---|---|---|---|
| F-01 | 施設設定は `src/data/station.ts` を唯一の情報源とし、ページ側に数値を複写しない | must | L1 |
| F-02 | 野辺山の気象平年値から、作型ごとの収穫日と「その日に何が並ぶか」を導出する旬カレンダー | must | L1 |
| F-03 | 架空である旨をフッタ / access / rv / JSON-LD の 4 箇所に明示し、番地・電話番号・地図のピン・道の駅の登録番号を一切持たない | must | L1（ゲート）/ L4 |
| F-04 | 直売所ページ（カテゴリ絞り込み＋旬カレンダー全景） | must | **L3 完了** |
| F-05 | レストランの品書きを F-02 の出力から組み立てる（サラダ＝その日の野菜＋ハム＋チーズ） | must | **L3 完了** |
| F-06 | ウッドデッキからの八ヶ岳稜線同定図（DEM から方位角・仰角・視通を計算） | must | **L2 完了** |
| F-07 | RV パーク（区画図・電源容量・当日先着の稼働可視化） | should | L4 |
| F-08 | 道のり（略図・小海線・冬季の注意） | should | L4 |
| F-09 | お知らせ（MDX、リポジトリ内） | should | L4 |
| F-10 | 明暗テーマ対応 | should | **L3 完了** |
| F-11 | OG 画像・JSON-LD・sitemap | should | L5 |

## 4. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | **外部の数値は原典を `data/` に vendor してから採用する。** 検索結果の要約・第三者サイトの転記を権威値にしない。候補生成にのみ使う | 生成器が原本を読む。原本は repo に入る |
| N-02 | **循環の禁止。** モデルのパラメータと、その検証に使うオラクルを同一の記述から取らない。**二つの product・二つの測定系を混ぜて差を取らない**（判定は片方で自己完結させ、もう一方は照合にだけ使う） | T-04 / T-05 / T-22 / T-23 |
| N-03 | 写真素材を一切持たない。ビジュアルは SVG のみ | T-25 / 出荷 HTML に `<img>` と外部参照が無いこと |
| N-04 | 外部 API に実行時依存しない（気象・地図とも静的データで完結） | 依存関係とネットワーク呼び出しの不在を検査 |
| N-05 | 出荷 HTML を読む検査（ソースでなくビルド生成物） | `scripts/verify-output.mjs`（**L3 完了**） |

## 5. 旬カレンダーのモデル（F-02）

### 5.1 出典

**BSI 生物科学研究所「実用作物栽培学」レタス**（`data/source/bsi_lettuce.pdf`、2026-08-27 取得）。

パラメータとして採るのは**本文の記述**:

- 「5 ℃以下の低温と 30 ℃以上の高温に於いて生育が停止する」（p.2）
- 「品種により定植してから早生種では 40 日、中生種では 50 日、晩生種では 60 日前後に収穫する」（p.2）
- 「結球しないリーフレタスは成長が速いので、定植してから 30〜40 日後、葉が 20〜30 枚展開したところに収穫する」（p.2–3）
- 「結球レタスは 10 ℃以下と 25 ℃以上になると球の肥大が抑制され、30 ℃を超えると結球しない」（p.2）

### 5.2 モデル

原典は温度をふたつの粒度で書いている。**取り違えると作期が丸ごとずれる。**

- p.1「生育温度範囲 15〜25 ℃で、**10 ℃以下**と 30 ℃以上では生育が**阻害**される」
- p.2「**5 ℃以下**の低温と 30 ℃以上の高温に於いて生育が**停止**する」

5 ℃ は「枯れずに耐える下限」、10 ℃ は「伸びる下限」である。
所要日数（定植後 40/50/60 日）は生育が阻害されない条件での日数なので、
積算すべきは阻害されない日 —— すなわち **10 ℃ 超 30 ℃ 未満**の日である。

日平均気温の平年値を日ごとに見て、**生育可能日**（10 ℃ < 日平均気温 < 30 ℃）を数える。
定植日から生育可能日を積算し、作型の所要日数に達した日を**収穫日**とする。
生育停止日は積算に加わらないので、収穫日は先送りされる。

単純な暦日の足し算ではなく、また「有効積算温度」でもない。
**原典が日数と停止温度でしか書いていない以上、モデルもその粒度を超えない**（HC-016）。

### 5.3 検証オラクル（N-02 循環の禁止）

パラメータを取った本文とは**別の記述**を検証に使う。

- **オラクル A**（同一 PDF の別図）: 図 2「各地の結球レタス栽培暦」寒冷地・冷涼地の行 —
  定植 5 月中旬〜8 月中旬 → **収穫 6 月下旬〜10 月中旬**。
  図 3「リーフレタス栽培暦」寒冷地・冷涼地 — 定植 4 月中旬〜8 月上旬 → **収穫 6 月上旬〜10 月上旬**
- **オラクル B**（第三者・南牧村公式）: `data/source/minamimaki_kogen-yasai.html`。
  野辺山高原のレタス出荷は 6 月下旬から 10 月まで

モデルに「図 2 の定植期間」を入力して得た収穫日が、
**図 2 の収穫期間と南牧村の出荷期の両方に収まること**を検査する。
どちらか一方を他方から生成した瞬間、この検査は何も検証しなくなる。

**オラクルの窓は旬（10 日）粒度でしか主張しない。** 栽培暦は棒グラフを目視で読む図であり、
日単位の精度を保証していない。窓を前後 1 旬ぶん緩めて当てる（HC-016「保証粒度を超えない」）。

#### この検査で実際に何が捕まったか（loop_001）

当初、生育可能日の下限に p.2 の 5 ℃ を採っていた。結球レタスは 5 ℃ 版でも 10 ℃ 版でも
収穫期がオラクルと 1 日差で一致し、**判別力を持たなかった**。
両者を切り分けたのは**リーフレタスだけ**である（5 ℃ 版は収穫開始 5/16 で、
原典の「6 月上旬」より 16 日早い）。オラクルが無ければこの取り違えは検出できなかった。

#### 独立性の限界（正直に書く）

閾値 10 ℃ は原典の記述だが、**5 ℃ と 10 ℃ のどちらを採るかをオラクルで決めた**。
その分、オラクルの独立性は消費されている —— 作期の**開始時期**について、
オラクル A はもはや独立な検証ではない。

独立性が残っているのは次の 3 つであり、ゲートの値はここにある:

1. 作期の**終端**（閾値の選択に使っていない）
2. 結球レタスとリーフレタスの**差**（同じ閾値で両方が窓に収まること）
3. **南牧村公式**という第三者出典（オラクル B。BSI とは無関係な情報源）

### 5.4 モデルの出力（2026-08-27 実測）

| 作型 | 所要日数 | 定植期間 | 導出された収穫期 |
|---|---|---|---|
| 早生の結球レタス | 40 | 5/11–8/20 | 6/20 – 9/29 |
| 中生の結球レタス | 50 | 5/11–8/20 | 6/30 – 10/9 |
| 晩生の結球レタス | 60 | 5/11–8/20 | 7/10 – 10/12 |
| リーフレタス | 35 | 4/11–8/10 | 6/11 – 9/14 |

晩生種は定植期間の遅い側 7 日ぶんが**収穫に至らない**。これはモデルの欠陥ではなく
野辺山の性質（晩夏に定植すると秋までに所要日数を積めない）なので、
「落ちるのは遅い定植だけ」という単調性で縛る（T-04 の 3 番目）。

### 5.5 L1 のスコープ

**レタス 4 作型（結球 早生・中生・晩生 / リーフ）のみ**を出荷する。

キャベツ・ダイコン・ブロッコリー・ホウレンソウの原典は `data/source/` に vendor 済みだが、
**キャベツの本文には「定植から収穫まで何日」の記述が無く、栽培暦しか無い**。
栽培暦をパラメータにも検証にも使うと循環する（N-02 違反）ので、L1 では出荷しない。
L3 で日数の一次記述を別出典から得るか、得られなければ
**「この作物にはオラクルが無い」と本節に明記したうえで**扱う（HC-012）。

## 6. 稜線同定図のモデル（F-06）

### 6.1 出典

| 何 | 原典 | 性質 |
|---|---|---|
| 稜線 | 国土地理院 標高タイル **DEM10B**（z=12・標本間隔 30.9 m） | 等高線由来の標高メッシュ |
| 峰 | 国土地理院「日本の主な山岳標高一覧（1003 山）」GeoJSON | **三角点・標高点の測量成果** |

**この二つは同じ発行者だが別の測量 product である。** 互いから生成していないので、
両者の一致がそのまま検証になる（N-02）。ただし発行者が同じである以上、
完全に独立した観測ではない —— この限界は明記しておく。

### 6.2 視点

架空の施設に座標は無い（F-03）。図は SPEC §2 が気象の基準点として既に採っている
**気象庁アメダス野辺山観測所の地点**（北緯 35°56.9′ 東経 138°28.3′）から計算する。
実在の公的な基準点であって、施設の所在ではない。サイト上にもそう書く。

目の高さは地表 + 1.5 m。DEM が示すその地点の地表標高は **1351.35 m** で、
気象庁が公表する観測所標高 1350 m と 1.35 m しか違わない。
これが座標変換とタイルの読み方が正しいことの、いちばん強い裏付けである。

### 6.3 モデル

方位 255°〜325° を 0.05° 刻みで走査し、各方位について射線を 15 m ずつ 26 km まで進めて
見かけの仰角が最大になる点を採る。これが稜線である。

見かけの仰角には**地球の丸みと大気差**を入れる（`(1−k)·d²/(2R)`、k = 0.13）。
入れないと 10 km 先で 0.04°、20 km 先で 0.08° ぶん高く見積もる。

**視通判定は DEM だけで自己完結させる。** 峰の仰角も DEM 標高から計算する。
遮蔽側を DEM、峰側を測量成果から取ると、両 product の系統差（DEM は山頂で 1〜40 m 低く出る）が
そのまま判定の余裕に化ける（loop_002 で実際に起き、赤岳が 0.09° 差で辛うじて残っていた）。

**山頂の手前 150 m は遮蔽の走査から除く。** DEM は標本間隔 30.9 m より細かい起伏を持たないので、
その範囲の地形は山頂そのものであって、遮るものではない。除かないと峰は自分の山頂に隠される。

### 6.4 結果（2026-08-27 実測）

方位 255°〜325° に入る 13 座のうち、**5 座が見え、8 座は手前の尾根に隠れる**。

| 峰 | 標高 | 方位 | 仰角 | 距離 | 判定 |
|---|---|---|---|---|---|
| 権現岳＜三ッ頭＞ | 2580 m | 266.0° | 7.16° | 9.7 km | 見える |
| 権現岳 | 2715 m | 271.0° | 7.66° | 10.1 km | 見える |
| 赤岳 | 2899 m | 285.3° | 9.23° | 9.5 km | 見える |
| 横岳 | 2830 m | 295.2° | 8.51° | 9.8 km | 見える |
| 硫黄岳 | 2760 m | 301.5° | 7.43° | 10.7 km | 見える |
| 編笠山 | 2524 m | 266.3° | 5.81° | 11.4 km | 三ッ頭の陰 |
| 阿弥陀岳 | 2805 m | 284.8° | 7.83° | 10.5 km | 赤岳の陰 |
| 天狗岳 | 2646 m | 307.0° | 5.59° | 13.1 km | 根石岳あたりの尾根の陰 |
| 蓼科山 | 2531 m | 317.5° | 2.78° | 23.5 km | 北八ヶ岳の陰 |

（ほかに 権現岳＜西岳＞・峰の松目・縞枯山・横岳〈北〉が隠れる）

**蓼科山や天狗岳が野辺山から見えない**のは目で見ただけでは分からない。
図が答えられるのはこの種の問いである。

### 6.5 検証オラクル

- **オラクル A** — 見える峰の方位が、DEM だけから決めた稜線の**局所極大**と一致するか。
  実測のずれは最大 0.15°（三ッ頭）。方位の刻み 0.05° の 3 倍を許容とする
- **オラクル B** — 隠れる峰は、同じ方位の稜線が必ずその峰より高いか。8 座すべてで成立
- **オラクル C** — DEM の山頂標高が測量成果を**上回らない**か。
  30.9 m メッシュは鋭い山頂を均すので必ず低めに出る。実測 −4.2〜−37.4 m。
  上回ったら座標変換かタイルの並べ方を疑う

### 6.6 目視は自動化しない

図の欠陥のうち二つは、**テストでは一切検出できなかった**（loop_002）。
名札の重なりと、方位ラベルが稜線の塗りつぶしに沈んで読めないこと。
どちらも SVG の構造としては正しく、DOM を検査しても通る。

`npm run render:panorama && npm run shot` で実ブラウザに描いて**明暗の両テーマを目で見る**。
これはループの手順であって、テストの代わりではない。

## 7. サイトの骨格（L3）

### 7.1 静的書き出しを採る

`output: "export"`。サーバ関数を持たないので、**実行時に外部へ問い合わせる経路が存在しない**（N-04）。
天気も地図も vendor 済みのデータから計算済みで、ページは結果を出すだけである。

姉妹作 [senoto-mori] は ISR と Server Actions を見せることが目的なので構成が違う。
こちらは「外部依存を持たないこと」自体が主張なので、静的に倒す。

### 7.2 経路

| 経路 | 中身 |
|---|---|
| `/` | 稜線の図・作期の要約・この作品について |
| `/shop/` | 旬の帯（通年 366 日）・品の一覧（分類で絞り込み）・分類の表 |
| `/restaurant/` | 品書きと出せる期間・夏と冬の比較 |
| `/panorama/` | 稜線の図・峰の一覧（見える／隠れる）・計算方法 |

### 7.3 絞り込みは表示だけの機能

分類の絞り込みはクライアント側の操作でしかない。初期状態が「すべて」なので、
**出荷 HTML には品が全件入る**。JS が動かない環境では絞り込めないだけで、品は全件読める。
出荷 HTML を読む検査（N-05）も全件を見られる。

### 7.4 明暗テーマ（F-10）

色は必ず `:root` に既定を持たせ、暗色ではトークンの**値だけ**を差し替える。
色をメディアクエリの中だけで定義しない —— 定義漏れがそのまま
「暗い背景に暗い図を置く」事故になる（フリートで 2 回起きている）。

`verify-output.mjs` が、**明色で定義されたトークンが暗色にも全て存在すること**を出荷 CSS で確かめる。

### 7.5 出荷 HTML を読む検査（N-05）

`npm run build && npm run verify`。ソースでなく `out/` の生成物を読む。
ソースに書いてあることが出力に届くとは限らない（SVG の `<title>` が React の制約で
丸ごと落ちた事故がフリートに実在する）。

見るもの: 走査対象の件数 / 架空明示が全ページに出ているか・本文にも出ているか /
電話番号・番地・登録番号の不在 / `<img>` と外部参照の不在 / `fetch` 等の不在 /
稜線の図と名札が本当に出荷されているか / 品と旬の帯が出荷されているか /
キリル文字・ハングルの不在 / 暗色トークンの網羅。

**5 種の違反を実際に注入して落ちることを確認済み**（架空明示の削除・電話番号・
写真の `<img>`・稜線パスの破壊・暗色トークンの欠落）。

### 7.6 目視は HTTP で行う

**`file://` で撮ってはならない。** 静的書き出しは CSS を絶対パス（`/_next/static/css/...`）で
参照するので、`file://` ではファイルシステムの根に解決されて読み込まれず、素の HTML を見ることになる。
`harness/shot-site.mjs` が `out/` を HTTP で配信してから撮り、
**撮る前に色トークンが空でないことを確かめる**（手続きが成立したことを確かめずに結論を読まない）。

## 8. 品質基準

- 平年値の生成器は、原本の統計期間（1991〜2020）と列見出しを照合してから読む。
  食い違ったら例外で落ちる（黙って古い値を使わない）
- 通年 366 日（気象庁は閏日の平年値も公表している）。日付の連番を検算する
- 新しいゲートは、わざと違反させて落ちることを確かめるまで信用しない

## 9. スコープ外

- 実在の道の駅・実在店舗の情報を載せること
- 実在地の写真素材の使用
- 実行時の外部 API 呼び出し（天気・地図とも）
- 予約の実処理（RV パークは当日先着の**可視化**まで。キャンプ場の予約は姉妹作 senoto-mori の領分）
- 道の駅の登録番号・登録年など、実在の登録制度に紐づく情報
