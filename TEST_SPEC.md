# TEST_SPEC.md — hoshihata

<!-- scaffold template v1.12.0 から展開(2026-08-27)。以後このファイルはプロジェクトが育てる -->

期待値には**出所**を必ず書く（HC-016）。SPEC の条項 / 実測 / 外部権威 のいずれか。
件数・行数は定数で書かず、不変量で書く。

## L1 — 平年値と旬カレンダー

| ID | 対象 | 期待値 | 期待値の出所 |
|---|---|---|---|
| T-01 | 平年値データの形 | 通年 366 日、月ごとの日数が暦どおり、日付が連番、平均気温に欠測なし | 実測（気象庁の日別平年値は閏日を含む。2026-08-27 に生成器が検出） |
| T-02 | 観測所メタ | 標高 1350 m / prec_no 48 / block_no 0415 / 統計期間 1991-2020 | 外部権威（気象庁 過去の気象データ検索の観測所定義） |
| T-03 | 平年値の妥当性 | 年平均が 7 ℃台。日平均が 30 ℃ 以上の日が 0 日。最寒月が 1 月 | SPEC §2（南牧村公式「年平均 8 度前後」「真夏でも 30 度を超えることはほとんどない」との整合） |
| T-04 | **オラクル A** — 結球レタス | 図 2 の定植期間（5/11–8/20）に定植したとき、早生・中生・晩生の収穫日がすべて **6/21–10/20** に収まる | 外部権威（BSI レタス 図 2 寒冷地・冷涼地の収穫期「6 月下旬〜10 月中旬」）。パラメータを取った本文とは別の記述 — SPEC §5.3 |
| T-05 | **オラクル A** — リーフレタス | 図 3 の定植期間（4/11–8/10）に定植したとき、収穫日がすべて **6/1–10/10** に収まる | 外部権威（BSI レタス 図 3「6 月上旬〜10 月上旬」） |
| T-06 | **オラクル B** — 出荷期 | 旬カレンダーで結球レタスが並ぶ日は 6 月下旬〜10 月に収まる | 外部権威（南牧村公式「高原野菜のふるさと」） |
| T-07 | 生育停止の効き | 冬に定植した仮想の作型は収穫日を持たない（生育可能日が所要日数に達しない） | SPEC §5.2（5 ℃以下で生育停止） |
| T-08 | 生育可能日の判定 | 5 ℃ ちょうど・30 ℃ ちょうどは生育可能日で**ない**（境界は開区間） | SPEC §5.2（原典「5 ℃以下」「30 ℃以上」） |
| T-09 | 単調性 | 同じ定植日なら 所要日数が長い作型ほど収穫日が遅い（早生 ≤ 中生 ≤ 晩生） | 不変量（モデルの定義から必然） |
| T-10 | 決定論 | 同じ入力で常に同じ出力。`Math.random` / 実時刻に依存しない | SPEC §6 |
| T-11 | 施設設定の単一情報源 | `src/data/station.ts` の標高が平年値観測所の標高と一致する | SPEC §2 |
| T-12 | **架空明示ゲート** | 出荷ソースに 番地・電話番号・道の駅の登録番号 の形が現れない。`Math.random` を使わない | SPEC F-03 / §7 |
| T-13 | ゲートの空振り防止 | T-12 の走査対象が 0 件でないことを先に assert する | HC（chikuma-seiki で走査 0 件の空振り合格が実在） |
| T-14 | 文字種検査 | 日本語本文にキリル文字・ハングルが混入しない | fleet-cyrillic-leak |

## L2 — 稜線同定図

| ID | 対象 | 期待値 | 期待値の出所 |
|---|---|---|---|
| T-20 | 標高の照合 | DEM の山頂標高は測量成果を**上回らない**。不足は 50 m 以内 | 実測 2026-08-27（13 座で −4.2〜−37.4 m、中央 −11.4 m）。符号の向きは 30.9 m メッシュが山頂を均すという物理から |
| T-20b | 視点の標高 | DEM が示す視点の地表標高が観測所標高 1350 m と 5 m 以内 | 外部権威（気象庁の観測所定義）。実測 1351.35 m |
| T-21 | 稜線の形 | 方位が厳密増加・範囲を覆う／隣接方位の仰角差が 0.2° 未満／全方位が 25 km まで被覆内 | 実測 2026-08-27（隣接差 中央 0.012°・最大 0.064°） |
| T-22 | **オラクル A** | 見える峰の方位が、DEM だけから決めた稜線の局所極大と **0.30° 以内** | 二つの product の照合。実測ずれ 最大 0.15°、方位の刻み 0.05° の 3 倍を許容 |
| T-22b | 同 | 稜線が指す距離が峰までの距離と 200 m 以内／仰角差が −0.30〜+0.10° で DEM 側が低い | 実測（距離ずれ 30 m 以内、仰角差 −0.15〜−0.04°） |
| T-23 | **オラクル B** | 隠れる峰は、同方位の稜線が必ずその峰より高い | 不変量。矛盾する組み合わせは存在しえない |
| T-23b | product の分離 | 視通判定が DEM だけで完結している（DEM 仰角 ≤ 測量仰角） | SPEC §6.3。混ぜると系統差が判定の余裕に化ける |
| T-24 | ゲートの実証 | 方位を 1° ずらすと局所極大との一致が崩れる | SPEC §7 |
| T-25 | 図の出力 | 名札の集合＝見える峰の集合／名札どうしの隙間 **≥ 8 px**／`<image>` や外部参照が無い／色を決め打ちしない／決定論 | SPEC F-06 / N-03 / F-10 |
| T-26 | 添え文 | 見える・隠れるの数を数え直して書いている | 図と文が食い違わない（HC-039 と同型の予防） |
| T-27 | 視点の性格 | 視点が気象の基準点として明示され、平年値の観測所と同一座標 | SPEC F-03（架空の施設に座標は無い） |
| T-28 | 同名の峰 | 山名が重複しても（横岳が 2 座）峰のキーは一意 | 実測（データに同名が実在する） |

**T-25 の「隙間 ≥ 8 px」は当初 `≥ 0` だった。** 押しのけを無効化しても 1.71 px の隙間で
通ってしまい、ゲートが落ちることを実証できなかった。**実証が不発だったこと自体が、
基準が要求を表していない証拠**だった（loop_002）。

## L3 — 直売所・食堂・出荷 HTML

| ID | 対象 | 期待値 | 期待値の出所 |
|---|---|---|---|
| T-30 | 直売所のデータ | id が一意／分類が全て定義済み／どの分類にも品がある／季節の品の作型 id が実在の作型を指す／**すべての作型がどれかの品に割り当たる** | 二重定義の照合。文字列で書き写した id は必ず腐る |
| T-31 | 品書きのデータ | id が一意／全素材が実在の品を指す／**すべての作型がどれかの品書きで使われる**／野菜を使わない品が一つ以上ある | 同上。最後の一つは「冬に品書きが空にならない」ことの担保 |
| T-32 | 棚 ＝ 旬カレンダー | 季節の品が並ぶ日が、その作型の収穫日と**完全に一致**／通年の品はどの日にもある／真冬の棚に高原野菜が 0 件 | SPEC §1「冬は何も並ばない」を計算で示していること |
| T-33 | 品書き ＝ 棚 | 揃わない品を出さない **かつ** 揃った品は必ず出す／どの日でも品書きが空でない／夏のほうが冬より長い | 二方向で縛る。前者だけなら「常に空を返す」実装が通る |
| T-34 | 決定論 | 同じ日で常に同じ棚と品書き／範囲外は例外 | SPEC §8 |
| T-35 | ゲートの実証 | 真冬の品書きに高原野菜を使う品が一つも無く、夏には出る | SPEC §8 |
| T-36 | 平年値から出る一言 | 年平均が 7 ℃台／30 ℃ 以上が 0 日／最寒 1 月・最暖 8 月／日最低の最小が −10 ℃ 未満 | SPEC §2。実測 −13.1 ℃（2026-08-27） |
| T-37 | 旬の帯の区間 | 区間が並ぶ日の集合をちょうど覆う／昇順で重ならず隣り合わない／作期が夏に寄り冬を含まない | 帯の図はこの区間から描かれる。ずれたら図が嘘になる |
| T-38 | 月の目盛 | 通日に比例した位置であり、**12 等分ではない**（2 月 1 日は 8.47 % であって 8.33 % ではない） | 帯が通日比例なので、等分すると最大 1.5 日ずれる（HC-039 と同型） |

### 出荷 HTML を読む検査（`npm run build && npm run verify`）

テストではなくスクリプト（`scripts/verify-output.mjs`）。ソースでなく `out/` を読む。

| 見るもの | 落ちることの実証 |
|---|---|
| 走査対象の件数と主要 4 ページの存在 | — |
| 架空明示が全ページに／本文にも出ているか | **実証済み**（文言を差し替えて赤） |
| 電話番号・番地・登録番号の不在 | **実証済み**（`0267-00-1234` を注入して赤） |
| `<img>` と外部参照の不在 | **実証済み**（`<img src="/yatsugatake.jpg">` を注入して赤） |
| 稜線の図・名札・`<title>` が出荷されているか | **実証済み**（`hh-ridge` を壊して赤） |
| 品と旬の帯が出荷されているか | — |
| キリル文字・ハングルの不在 | — |
| 暗色トークンの網羅 | **実証済み**（`--ridge` を落として赤） |

## L4 — RV パーク・道のり・お知らせ

| ID | 対象 | 期待値 | 期待値の出所 |
|---|---|---|---|
| T-40 | 区画のデータ | id が一意／区画どうしが重ならない／電源付きと電源なしの両方がある | 図に描く以上、重なっていたら図が嘘になる |
| T-41 | **循環の禁止** | 人手で書いた `deckSide` が、デッキとの距離から独立に計算した結果と一致／両クラスが非空 | senoto-mori G-03 と同型。一方を他方から生成したら検証にならない |
| T-42 | **電源容量のオラクル** | 総当たり 2^7 = 128 通りを漏れなく列挙／最大構成が独立に書いた素朴な走査と一致／全部は入らない | 総当たりの厳密解。近似も枝刈りも使わない |
| T-42b | 実測値 | 冷蔵庫 45 + ケトル 1000 + 炊飯器 700 = 1745 W > 1500 W | 実測（データの定格から） |
| T-43 | 気温で要るものが変わる | 真冬に電気毛布が要り、真夏には要らない／暖房を足しても収まる組み合わせが残る | 決め打ちでなく気象平年値から決まる |
| T-44 | **混み具合の 4 性質** | 決定論／保存則（0〜区画数）／偏りの不在／需要の向き（夏の土曜 > 冬の平日、土曜 ≥ 水曜） | senoto-mori G-05。作り物だからこそ性質で縛る |
| T-45 | 見込みの言葉と表 | 三段階が値域を過不足なく覆う／12 か月そろう／どの月も土曜 ≥ 平日 | 不変量 |
| T-46 | ゲートの実証 | 常に 0 を返すモデルは値域が 1 種類しかなく「偏りの不在」を満たさない | SPEC §9 |
| T-46b | **`<title>` が空でない** | 場内図・方位盤の `<title>` がすべて中身を持つ／複数式で書くと実際に落ちることの実証 | HC-037 の再発。本作でも 22 個が空になっていた |
| T-47 | 場内図の二重定義 | 描画結果の区画集合＝区画データ／電源とデッキ側の属性も一致／名前が `<text>` に出ている／**出荷 HTML にも届いている**／viewBox が全区画を含む | senoto-mori G-01。ソースを grep しても意味がない |
| T-48 | 方位盤の二重定義 | 描画結果の峰集合＝範囲内の峰／番号が方位順に 1 から／峰の名前は図に出さない／**`<line>` は方位の軸 2 本だけ**／出荷 HTML にも届いている | F-03（実在の経路を案内図にしない） |
| T-49 | 一覧と原稿の照合 | 一覧の全項目に原稿がある **かつ** 一覧に無い原稿が残っていない／新しい順／原稿が題名を二重に持たない／外部リンクを持たない／本文のリンク先が実在の経路 | sugi-nami の news.test.ts と同型。双方向で塞ぐ |

### 出荷 HTML を読む検査（L4 で追加）

| 見るもの | 落ちることの実証 |
|---|---|
| 場内図の区画・電源の表・`<title>` が出荷されているか | — |
| 方位盤の峰・`<title>` が出荷されているか | — |
| お知らせの一覧と記事が双方向で一致するか | — |
| **空の `<title>` が出荷されていないか** | **実証済み**（修正前の出荷物で 22 個を検出して赤） |

空 `<title>` のゲートは**注入した違反ではなく、実在した欠陥**で落ちることを確認している。

## L5 — 公開

| ID | 対象 | 期待値 | 期待値の出所 |
|---|---|---|---|
| T-50 | **OG のフォント網羅** | 使う字が台帳にすべて入っている／woff が二つとも空でない／台帳に無い字（鬱）は入っていない／全経路に文言がある | 「文言を変えた瞬間に豆腐になる」の予防。台帳は生成器が書く |
| T-51 | **JSON-LD の禁止事項** | 事業所・場所を主張する 12 の型を出さない／住所・座標・電話・営業時間の 11 項目を出さない／説明文に架空である旨がある／出してよい型は出ている | 架空の施設に事業所情報を出さない（F-03） |
| T-51b | ゲートの実証 | `LocalBusiness` + `telephone` を混ぜると検出される | SPEC §10 |
| T-52 | sitemap の経路 | 経路がデータ側にある／URL が https で末尾スラッシュ無し／経路が `/` で始まり `/` で終わる | `trailingSlash: true` に揃える |

### 出荷 HTML を読む検査（L5 で追加）

| 見るもの | 落ちることの実証 |
|---|---|
| OG 画像が 7 経路ぶん PNG として出荷されているか | **実証済み**（中身を壊して赤） |
| JSON-LD が事業所を主張していないか | **実証済み**（`LocalBusiness` を混ぜて赤） |
| sitemap に出荷した全経路が載っているか | **実証済み**（`/rv/` を落として赤） |
| 外部の**資産**を取りに行っていないか | **実証済み**（`<link href="https://cdn…">` を注入して赤） |
| 外部リンクがフッタ規約の 5 項目だけか | **実証済み**（規約外リンクを注入して赤） |
| フッタ規約の 5 項目が全ページに揃っているか | **実証済み**（1 項目を消して赤） |

注入は**成立を assert してから**確認している。注入が成らないまま「違反なし」が出た事故が loop_003 で実在した。

## L6 — 宿題の解消

| ID | 対象 | 期待値 | 期待値の出所 |
|---|---|---|---|
| T-53 | **デグレード防止** | 作型の一般化でレタスの収穫期が動かない（早生 6/20–9/29 / 中生 6/30–10/9 / 晩生 7/10–10/12 / リーフ 6/11–9/14） | 実測 2026-08-27（SPEC §5.4）。拡張の前に固定してから着手した |
| T-54 | 作物ごとの閾値 | キャベツ 8–28 ℃、レタス 10–30 ℃／全作型が日数と閾値の出所を持つ／耐える下限 < 伸びる下限／閾値を渡すと判定が変わる | SPEC §5.5。一律の閾値を置かない設計をテストで固定 |
| T-55 | **オラクル** キャベツ | 栽培暦（春 7 月上旬〜8 月中旬／夏秋 9 月上旬〜10 月中旬）の窓に収まる／春が夏より先／レタスより日数が長い | 外部権威（BSI キャベツ 図 2）。パラメータを取った p.12 本文とは別の記述 |
| T-56 | **オラクルの判別力を測る** | 9 組合せ中 8 つが窓に収まる（＝弱い）／外れるのは 5 ℃ × 60 日の 1 件／対照としてレタスでは効く | 実測 2026-08-27。**弱いことを確かめる検査**。強くなったら SPEC §5.6 を書き直す合図 |
| T-57 | キャベツが棚と品書きに届く | 7/1〜10/14 に並ぶ／レタスが終わったあとも残る／真冬には並ばない | 実測。作物を足した意味がデータに出ていること |
| T-29 | **判定不能を出力に残す** | 判定不能の峰が実在する（分岐が生きている）／判定不能なのは射線が届かないか DEM 被覆外の峰だけ／**射線が届く峰はすべて判定されている**（逃げ道に使っていない）／判定不能の峰は仰角の値を持たない／採録半径 > 射線の距離 | SPEC §6.4。L5 まで一度も通らなかった分岐 |

### 操作の検査（`npm run build && npm run verify:ui`）

テストでも出荷 HTML 検査でもない。実ブラウザでボタンを押す（`harness/verify-ui.mjs`）。

| 見るもの | 落ちることの実証 |
|---|---|
| 手続きの成立（色トークン・ボタン数・品の描画） | — |
| 初期状態が「すべて」で、出荷 HTML と描画後の品数が一致 | **実証済み**（初期状態を絞り込み済みにして赤） |
| 各分類を押すと、その分類だけが正しい件数で残る | **実証済み**（絞り込みを効かなくして赤） |
| 「すべて」に戻すと全件に戻る／Tab で焦点が移る／コンソールにエラーが無い | — |

## L7 — 残り 3 作物の確認

| ID | 対象 | 期待値 | 期待値の出所 |
|---|---|---|---|
| T-58 | **オラクル** ホウレンソウ | **播種**から数える作型である（定植ではない）／播種期間 4/11–9/20 が原典の旬と一致／収穫が 5 月下旬〜10 月中旬に収まる／播種期間の**全日**で成立する／閾値 5–25 ℃ が他作物より上下とも低い／いちばん早く棚に出る | 外部権威（BSI ホウレンソウ p.2）。パラメータを取った p.9 / p.1 とは別の記述 |
| T-59 | **作物名が型で縛られている** | すべての作物に日本語の呼び名がある／呼び名に英字が混ざらない／一覧が出荷作物と過不足なく一致／作期の起点にも呼び名がある | loop_007 で「レタスとキャベツと spinach」が出荷された。`Record<Crop, string>` で埋め忘れをコンパイルエラーにした |
| T-37（更新） | 作期の帯 | 5/16–10/25（163 日）。旬粒度で緩めて縛る | 実測 2026-08-27（L7 時点）。ホウレンソウを足して作期が早まった |

### 出荷 HTML を読む検査（L7 で追加）

| 見るもの | 落ちることの実証 |
|---|---|
| **内部の識別子が本文に漏れていないか**（`lettuce` / `sowing` / `undefined` / `[object Object]` 等） | **実証済み**（「ホウレンソウ」を `spinach` に差し替えて赤。注入の成立を tag 除去後の可視テキストで assert してから確認） |

属性値（`data-*` や `class`）に識別子が出るのは正常なので、
**タグを除いた可視テキストだけ**を見る。

## L8 — 本番に対する検品（`npm run verify:live`）

テストでも出荷 HTML 検査でもない。**本番 URL への実リクエスト**で見る。
配信時のヘッダは、デプロイして実際に取得するまで存在しない情報である。

| 見るもの | 落ちることの実証 |
|---|---|
| 主要 10 経路が 200／架空明示が入っている／`<img>` が無い | — |
| **OG 画像 7 枚が `image/png` で返る** | **実際の欠陥で検出**（修正前は `application/octet-stream`。注入ではない） |
| OG 画像が PNG のシグネチャを持つ | — |
| `sitemap.xml` / `robots.txt` の `Content-Type` | — |
| `meta` の `og:image` が指す URL が実際に画像として取れる | — |
| 実ブラウザで CSS が効き、絞り込みが動き、コンソールにエラーが出ない | — |

判定に `vercel ls` や管理画面を使わない。

## 未検証（オラクルが無いもの）

- **ダイコン**（SPEC §5.8）。日数（播種後 50〜60 日）は原典にあるが、
  **下限温度の記述が無い**ので出荷しない。推測で埋めない
- **ブロッコリー**（SPEC §5.8）。日数も温度（10〜28 ℃）も原典にあるが、
  野辺山では 10 ℃ 超の日が 158 日しかなく作期が足りない。**測って出荷しないと決めた**
- **モデルの粗さ**（SPEC §5.9）。生育可能日を二値で数えるので、
  6 ℃ の日と 18 ℃ の日を同じ 1 日として扱う
- **キャベツのオラクルは判別力が弱い**（SPEC §5.6）。窓に収まることは確かめられるが、
  パラメータが正しいことの証拠にはならない。この弱さ自体は T-56 が固定してある
- **南アルプス方面の視通**（方位 207〜232°）。峰は採録するようになったが、
  DEM の被覆が届かないので `unknown` のままである。判定するには 88 タイル・45 MB が要る
